# Write your MySQL query statement below
SELECT DISTINCT v.customer_id, COUNT(v.visit_id) as count_no_trans FROM Visits v LEFT JOIN Transactions T ON v.visit_id = T.visit_id WHERE T.transaction_id IS NULL GROUP BY v.customer_id ORDER BY v.customer_id; 